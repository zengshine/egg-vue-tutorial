/**
 * @file 定义全局配置项
 */

// import $router from '@/router';
// import $api from '@/service';
// import { transform as $_ } from '@/utils/language';

// export default function globalConfig(vueInstance) {
//   Object.assign(vueInstance.config.globalProperties, {
//     $api, // api请求对象
//     $_, // 国际化翻译方法
//     $router, // 路由实例
//   });
// }
